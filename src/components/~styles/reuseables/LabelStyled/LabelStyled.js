import styled from 'styled-components';
import { dark, small_font_size } from '../index';

export const LabelStyled = styled.label`
	font-size: ${small_font_size};
	line-height: 1.4;
	font-weight: 400;
	color: ${dark};
`;
